# Changelog

## 0.1.1

Correcao de manifest: na 0.1.0 o mod instalava mas nunca aparecia na lista.

- `game_version` passou de `<1.0.0` para `<2.0.0`. O aplicativo e a versao
  1.8.0, entao a faixa antiga o excluia. E o mesmo motivo pelo qual o
  versaovermelha tambem nao aparece.
- `category` passou de `LANGUAGE` para `GAMEPLAY`. Nenhum mod instalado usa
  LANGUAGE; a outra traducao usa GAMEPLAY.
- Removidos `games` e `language`: nenhum mod que funciona os declara.

## 0.1.0

- Primeira versao: 1039 falas em portugues.
- Nomes de golpes mantidos no original.
